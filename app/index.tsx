// Phase 0 only. This route exists to host the Stage 0 accuracy rig and is replaced
// by the real Home screen once the spike passes and `spike/` is deleted.
export { default } from '../spike/rn-whisper/RigAScreen';
