// Phase 0 measurement rig. Throwaway, and not linked from anywhere in the app.
// Reachable at /rig, which is how a model choice gets re-measured. Deleted with
// spike/ once there is nothing left to measure.
export { default } from '../spike/rn-whisper/RigAScreen';
